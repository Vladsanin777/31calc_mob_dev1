<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Фон окна приложения -->
        <item name="android:windowBackground">@color/black</item>
        <!-- Основной цвет (акцент для кнопок, текста и анимаций) -->
        <item name="colorPrimary">@color/purple0</item>
        <item name="colorPrimaryVariant">@color/purple1</item>
        <item name="colorAccent">@color/purple2</item>
        <!-- Цвет текста по умолчанию -->
        <item name="android:textColor">@color/purple0</item>
    </style>
</resources>